/**
 * @module utils
 *
 * @desc Exports general use-case utilities for the cli and all modules.
 */

async function filterPackageDependencies(packageJson, projectType) {
    return null;
}

module.exports = {
    filterPackageDependencies
};
